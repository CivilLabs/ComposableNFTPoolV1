contract ComposableNFTVault {
    // store mapping(vaultId => CAddress, collateralNFTAddress, currentNum, layerCount, layerLengths, isOnlyForVault, is1155)

    // Deposit NFT to vault
    // mint and get a bottle

    // Withdraw NFT from vault
    // use target bottle and related layerData to withdraw
}